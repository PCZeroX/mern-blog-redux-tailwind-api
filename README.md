# API Blog

```TS
npm install bcrypt cookie-parser cors date-fns dotenv express express-async-errors express-rate-limit jsonwebtoken mongoose mongoose-sequence morgan uuid
```

```TS
npm install -D tailwindcss autoprefixer postcss
```

```TS
npx tailwind init -p
```

```TS
npx tailwindcss -i ./public/css/tailwind.css -o ./public/css/styles.css --watch
```

## Create a token

```TS
node

require("crypto").randomBytes(64).toString("hex")
```

```CSS
body {
  background-image: url("../img/background.jpg");
  background-position: center;
  background-size: cover;
  background-repeat: no-repeat;
  background-blend-mode: multiply;
  background-color: #000;
  background-attachment: fixed;
  color: #fff;
}
```

---
